
const config = {
    preset:"ts-jest",
    testEnvironment:"node",
    testMatch:["**/*.test.ts"],
    testTimeout:30000,
    forceExit:true,
    moduleFileExtensions:["ts","js"],
    rootDir:"",
}

export default config
